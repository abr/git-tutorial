# Put these aliases in .bashrc, .zshrc, or wherever you configure your shell

alias gra='git rebase --abort'
alias grc='git rebase --continue'
alias gri='git rebase --interactive'
alias grs='git rebase --skip'
